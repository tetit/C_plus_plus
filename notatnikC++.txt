#include<bazwa_biblioteki> (np. iostream) <iostream> 

#include <windows.h>
Sleep(1000); - czas w milisekundach 1c = 1000 milesek

#include <cstdlib>
system("cls"); - wyczysczienie ekranu

#include <iomanip>
cout<<setprecision(10000); (кол-во цифр числа на экране)

cout << "Napis" << endl; (odczytac)

int (integralna, niepodzielna)

cin >> x; (wczytac)

= (operator przypisania)

 #include <iostream>
 #include <cstdlib>
 #include <time.h>
 #include <stdio.h>
 #include <windows.h>
system("pause"); пауза до нажатия любой клавиши
getchar(); getchar(); пауза до нажатия любой клавиши
srand(time(NULL)); (задается число 1 раз первое из процессора времени)
 liczba=rand()%100+1;(случайное чило от 1 до 100)
system("cls"); wyczyszanie ekranu

#include <conio.h>
char a;
a=getch(); выбор нажатием любой клоавиши на клавиатуре

cerr<<"To nie jest liczba!";   комуникат о ошибке #include <cstdlib> <cctype> biblioteka walidacji adresow i znakow
        exit(0);

#include <fstream> - biblioteka plikow tekstowych
fstream nazwa_pliku; - stworzenie zminnej pliku w kodie zrodlowym
plik.open("wizytuwka.txt", ios::out); - otwartie i otczyt pliku, stworzenie albo perezapisanie wizytuwka.txt
plik<<imie<endl; - zapis do pliku
plik.close(); zakytie pliku
plik.open("wizytuwka2.txt", ios::in); - zapis w isnujuczyj plik
 if(plik.good()==false)
    {
        cout<<"plik nie istnije!";
        exit(0);
    } //sprawdzienie czy istnie plik

nr_tel = atoi(linia.c_str()); konwertowacz string na int (z napisu na liczbe

#include <algorithm> #include <string>

.length() - dlugosc string
"a" - jest napis (a \0) miejsce w pamienci
'a' - jest pojedynczyj znak (a) miejsce w pamienci

getline(cin, nazwa_string); zapisuje napisy ze spacjami

